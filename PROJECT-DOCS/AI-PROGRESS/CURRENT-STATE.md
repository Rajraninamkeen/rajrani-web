# CURRENT-STATE — BILOKAT

> Authoritative AI snapshot. Update at the end of every session.
> Never mark anything COMPLETE without verification evidence.

| Field | Value |
|---|---|
| Project | BILOKAT — Multi-Seller Marketplace Platform |
| Canonical repo | `Rajraninamkeen/rajrani-web` (all work lives here; owner decision) |
| Remote | `origin https://github.com/Rajraninamkeen/rajrani-web.git` |
| Branch | `main` |
| Layout | `PROJECT-DOCS/` (specs + AI-PROGRESS) · `landing-page/` (frontend prototype) · backend at repo root (`src/`, `prisma/`) |
| Stack (final) | **NestJS 11.2.3 + TypeScript 5.9.3 + Prisma ORM 7.10.0** (driver adapter, prisma.config.ts) + PostgreSQL 17 |
| Current session | Session 37 — **Period / date-range finance views** (back-office) done & pushed. Owner picked the period-views option of the payout-schedule / period-based-finance topic and narrowed it to the additive **From/To date-range finance views**: optional inclusive `from?/to?` query params on the operator finance reads — seller payables by `earnedAt`, settlements by `createdAt`, courier payout ledger **and** summary by `earnedAt`, and the overview per-seller report totals (already `earnedAt`-aware) — implemented via a single shared private `dateFilter` helper (`settlement.service.ts`, `courier-payout.service.ts`) whose date-only `to` expands to inclusive end-of-day and whose unparseable values 400. `finance.dto.ts` `PayableListQuery`/`SettlementListQuery` gained `from?/to?`; controllers forward them. **No schema / money-model change.** Frontend `FinanceOps.jsx` gained a **Period (From/To)** bar (`Apply` / `Clear` + active caption) scoping every list & KPI (payables, settlements, courier ledger + pending/paid summary, report totals/net-payable labels switch to "(period)"); reconciliation stays global (fetched once on mount). `+5` unit tests on window shaping (affected suites green); `npm run typecheck`/`nest build`/console `npm run build` clean; `scripts/live-period-verify.mjs` read-only E2E on `:4600` — scoped reads 200, invalid dates 400. |
| Current phase | Seller split-checkout + per-seller fulfilment gate + partial-fulfilment + seller payables/settlements + finance reconciliation & reporting + seller onboarding/KYC (orgs/applications/documents/review/activation/REVIEWER) + per-slice courier delivery (delivery_partners/assignments/events + DELIVERY role) + **return replacement/exchange vs refund with customer photo evidence upload** + **outbound replacement dispatch** (item-level returns) + **real Razorpay payment-gateway provider with LIVE refund execution (E2E vs a local Razorpay-protocol mock)** + **async gateway refund reconciliation (`refund.processed`/`refund.failed` → finalise PROCESSING refunds)** + **product listing/publishing (seller authors DRAFT → submits PENDING_REVIEW → OPERATOR/ADMIN approve → APPROVED+LIVE into the public catalog, or reject with a surfaced reason; immutable per-product status history)** + **product reviews/ratings (verified-buyer reviews: only a CUSTOMER who received the product reviews it; PENDING → OPERATOR/ADMIN moderate to PUBLISHED/HIDDEN/REJECTED; only PUBLISHED reviews are public; Product.ratingAvg/reviewCount recomputed from PUBLISHED reviews)** all done; **courier last-mile delivery of the dispatched replacement (Session 22: OPERATOR assigns a DELIVERY partner to a DISPATCHED replacement; the courier's `deliver` auto-completes it, DISPATCHED→COMPLETED; non-money leg)**; connected frontend UIs now exist too — **Session 23 catalog console** (SELLER product authoring + OPERATOR/ADMIN publishing review in one React/Vite app) and **Session 24 customer storefront** (live catalog browse + product detail + PUBLISHED reviews + CUSTOMER sign-in with a review-write surface) and **Session 25 customer commerce** in the same app (guest + signed-in cart, checkout that drives the live /cart, /checkout/preview, /orders and /orders/:id/cod endpoints, plus PREPAID order placement with a dev-only sandbox capture harness). Follow-ons that remain: real delivery-courier provider integration (tracking/POD/per-slice payout) and other owner-chosen priorities. Session 26 added a **staff review-moderation UI** (an OPERATOR/ADMIN "Review Moderation" tab in the catalog console over the live /product-reviews routes); Session 27 added a **SELLER sales & payouts dashboard** (a SELLER "Sales & Payouts" tab: slice accept/reject over /seller/orders + payables/settlements over /seller/payables + /seller/settlements); Session 28 added an **OPERATOR Operations console** (an OPERATOR/ADMIN "Operations" tab with order-fulfilment + returns queues over new staff read endpoints `/ops/orders`, `/ops/orders/:id`, `/ops/returns`, `/ops/returns/:id`, driving the existing fulfilment-advance and return-ops action routes). Session 29 added the DELIVERY-role "My Deliveries" courier console (slice parcels + replacement tasks); Session 30 added the pluggable external-courier-provider seam (tracking waybill on pickup + POD on deliver, sandbox default + real REST http against a local courier-protocol mock); Session 31 added the customer/operator **courier tracking-read surface** — `GET /orders/:id/tracking` calling the courier provider `track` live per waybill, surfaced as a "Track delivery" timeline in the customer storefront order detail (read-only, non-money); Session 32 added the courier delivery-fee payout / money leg: CourierPayout EARNED→SETTLED ledger on actually-delivered courier legs (parcel ₹35 / replacement ₹40 via COURIER_FEE_* env), accrued atomically in the deliver transaction + idempotent per assignment; DELIVERY self-service GET /delivery/payouts + OPERATOR/ADMIN list/summary/settle; roles enforced; DI fix for @Optional courierPayout injection; additive migration 20260908210000_courier_payout) |
| Overall status | Backend (foundation, auth/RBAC, catalog, commerce cart/checkout/orders, buy-now + sandbox payments + COD OTP, fulfilment/delivery, returns/refunds incl. item-level with replacement/exchange resolution + evidence upload + outbound dispatch lifecycle, multi-seller split-checkout + seller-ops + per-seller fulfilment gate + partial-fulfilment resolution + seller payables/settlements + finance reconciliation/reporting with auto return-debit + seller onboarding/KYC with Organizations + OWNER members + REVIEWER + per-slice courier delivery with a DELIVERY role + a real Razorpay payment-gateway provider with live refund execution + async refund reconciliation + product listing/publishing state machine + product reviews/ratings with verified-buyer + moderation + courier last-mile delivery of a dispatched replacement (Session 22: DELIVERY-partner assignment + courier-driven DISPATCHED→COMPLETED)) on Prisma 7; all verified — plus two connected React/Vite frontend UIs that exercise those routes through the Vite `/api` dev proxy: **Session 23 catalog console** (`catalog-console/`, SELLER authoring + OPERATOR/ADMIN publishing review) and **Session 24 customer storefront** (`customer-storefront/`, live public-catalog browse + product detail + PUBLISHED reviews + CUSTOMER sign-in/review-write). Session 31 adds a customer/operator courier **tracking-read** surface (`GET /orders/:id/tracking` → live provider track per courier leg, slice + replacement) with a "Track delivery" timeline in the storefront order detail (read-only, non-money); Session 32 adds the courier delivery-fee payout / money leg (CourierPayout EARNED→SETTLED ledger on actually-delivered legs, DELIVERY self-service read + OPERATOR/ADMIN settle; additive migration) |
| Completed sessions | 00–04, upgrade pass, 05 (buy-now + payments + COD), 06 (fulfilment/delivery), 07 (returns/refunds), 08 (item-level returns + pickup/inspection lifecycle), 09 (seller orgs + split-checkout + seller-ops core), 10 (per-seller fulfilment gating + delivery markers), 11 (partial fulfilment resolution of rejected seller slices), 12 (seller payables & settlements), 13 (finance reconciliation + reporting), 14 (seller onboarding/KYC + Organizations + REVIEWER), 15 (per-slice courier delivery + DELIVERY role), 16 (return replacement vs refund + evidence upload), 17 (outbound replacement dispatch), 18 (real Razorpay gateway + live refund execution E2E), 19 (Razorpay async refund reconciliation: `refund.processed`/`refund.failed` webhooks finalise in-flight PROCESSING refunds), 20 (product listing/publishing: seller-authored DRAFT → PENDING_REVIEW → OPERATOR/ADMIN approve/reject with surfaced reason; APPROVED+LIVE publishes into the existing public catalog; product_status_history audit), 21 (product reviews/ratings: verified-buyer review (DELIVERED purchase) → PENDING → OPERATOR/ADMIN moderate approve/reject/hide/unhide; only PUBLISHED reviews public; Product.ratingAvg/reviewCount recomputed from PUBLISHED reviews; unique productId+userId), 22 (courier last-mile delivery of a dispatched replacement: OPERATOR/ADMIN assigns a DELIVERY partner via `replacement_assignments`; the partner drives it on `/delivery/replacement-tasks` and its `deliver` auto-completes the replacement DISPATCHED→COMPLETED — a non-money leg, audited by DELIVERY-actor ReturnEvents), 23 (catalog console UI: a connected React/Vite app `catalog-console/` giving SELLER product authoring + OPERATOR/ADMIN catalog-publishing review in one role-aware app, speaking through the Vite `/api` proxy), 24 (customer storefront UI: a connected React/Vite app `customer-storefront/` that browses the live public catalog, shows product detail with PUBLISHED reviews + rating aggregate, and offers CUSTOMER sign-in with a verified-buyer review-write surface and "my reviews" page), 25 (customer commerce UI: guest + signed-in cart with line-level update/remove, checkout preview, order placement (COD & PREPAID), COD OTP authorization, "my orders"/order detail, plus a dev-only sandbox capture harness that keeps the webhook secret server-side), 26 (staff review-moderation UI: an OPERATOR/ADMIN "Review Moderation" tab in the catalog console that lists PENDING/PUBLISHED/REJECTED/HIDDEN customer reviews and moderates them — approve/reject (pending), hide/unhide (published/hidden) — driving the live /product-reviews routes), 27 (SELLER sales & payouts dashboard: a SELLER "Sales & Payouts" tab in the catalog console over the live /seller/me, /seller/orders (+ accept/reject a PLACED slice), /seller/payables and /seller/settlements routes, with KPI cards + payables table), 28 (OPERATOR Operations console: an OPERATOR/ADMIN "Operations" tab in the catalog console with order-fulfilment (advance a live order) + returns (decide/pickup/inspection/refund/replacement) queues over new staff read endpoints /api/v1/ops/orders[/:id] and /api/v1/ops/returns[/:id], driving the existing fulfilment-advance and return-ops action routes; backend typecheck/build clean), 29 (DELIVERY courier task console: a DELIVERY-role "My Deliveries" tab in the catalog console running live courier tasks over /delivery/tasks (slice parcels) + /delivery/replacement-tasks (outbound replacements) with accept/reject(reason)/pickup/out-for-delivery/deliver/fail(reason) bound to the courier's own profile; the slice-task courier surface was enriched so it is not hollow — a new DELIVERY-only GET /delivery/tasks/:assignmentId returns orderNumber/paymentMethod/sellerName/sellerCode + items[] (productName/weight/sku/qty) + customer{name,phone,address} + events, and the partnerTasks list adds orderNumber/itemCount/customer; backend typecheck/build clean, console vite build clean), 30 (pluggable courier-provider integration: a Razorpay-style `courier` seam — `src/commerce/courier` `CourierProvider` (createShipment/track/confirmDelivery) with a default deterministic Sandbox provider and a real REST HTTP provider selected via `COURIER_PROVIDER` + `COURIER_BASE_URL`/`COURIER_API_KEY`; courier `pickup` books a provider waybill (tracking) and `deliver` captures a POD, persisted on delivery_assignments + replacement_assignments via migration `20260908190000_courier_tracking`; assignment serializers expose carrier/trackingNumber/trackingUrl/podRef/podSignedBy/podAt; verified live over sandbox AND real-HTTP against a local courier-protocol mock; non-money — per-slice payout deferred, seller earn stays order-level; 22 suites / 214 tests; typecheck/build clean), 31 (courier tracking-read surface: a read-only `GET /orders/:id/tracking` aggregates an order's slice-parcel + replacement courier legs (latest per slice/replacement) and calls the configured `CourierProvider.track` LIVE per waybill, merged with local leg state + POD; order owner CUSTOMER or OPERATOR/ADMIN may read (cross-owner 404, wrong role 403); connected customer-storefront OrderView "Track delivery" timeline; read-only NON-money; 23 suites / 222 tests; typecheck/build clean; 14/14 live HTTP-provider E2E), 32 (courier delivery-fee payout / money leg: CourierPayout EARNED→SETTLED ledger on actually-delivered courier legs, parcel ₹35 / replacement ₹40 via COURIER_FEE_* env, accrued atomically in the deliver tx + idempotent per assignment; DELIVERY self-service GET /delivery/payouts + OPERATOR/ADMIN list/summary/settle; roles enforced; DI fix for @Optional courierPayout injection; additive migration 20260908210000_courier_payout), 33 (infra packaging + baseline security hardening: CI/CD GitHub Actions + Docker/prod-compose containerisation landed just before and pushed `d38ea0c`; this session added always-on security response headers via a global securityHeadersMiddleware and an in-memory sliding-window rate limiter (@Throttle+ThrottleGuard, HTTP 429, keyed ip|method|route) on credential/account-creation/OTP endpoints — register/seller-register 10/hr, login 8/min, refresh 60/min, COD otp+verify 5/min — with baseline bcrypt/strong-password/whitelist-validation/generic-login-error/JWT-session revocability confirmed present; new throttle.guard.spec.ts + security-headers.middleware.spec.ts; 26 suites / 238 tests; typecheck/build clean), 34 (back-office Finance console: an OPERATOR/ADMIN catalog-console “Finance” tab unifying courier-payout settlement (/delivery/payouts summary/all/settle — Session 32) and the seller payables/settlements ledger (/finance payables/adjust/settlements/create/advance + reconciliation + report totals — Sessions 12/13): Overview & Reconciliation, Courier Payouts (per-partner Settle EARNED→SETTLED), Seller Payables (signed adjustment form), Seller Settlements (create grouped by seller + advance with only legal next states); frontend-only over existing OPERATOR/ADMIN routes; scripts/e2e-finance.mjs 32/32 live E2E with a throwaway money round-trip + full DB self-clean) || Completed sessions | 00–04, upgrade pass, 05 (buy-now + payments + COD), 06 (fulfilment/delivery), 07 (returns/refunds), 08 (item-level returns + pickup/inspection lifecycle), 09 (seller orgs + split-checkout + seller-ops core), 10 (per-seller fulfilment gating + delivery markers), 11 (partial fulfilment resolution of rejected seller slices), 12 (seller payables & settlements), 13 (finance reconciliation + reporting), 14 (seller onboarding/KYC + Organizations + REVIEWER), 15 (per-slice courier delivery + DELIVERY role), 16 (return replacement vs refund + evidence upload), 17 (outbound replacement dispatch), 18 (real Razorpay gateway + live refund execution E2E), 19 (Razorpay async refund reconciliation: `refund.processed`/`refund.failed` webhooks finalise in-flight PROCESSING refunds), 20 (product listing/publishing: seller-authored DRAFT → PENDING_REVIEW → OPERATOR/ADMIN approve/reject with surfaced reason; APPROVED+LIVE publishes into the existing public catalog; product_status_history audit), 21 (product reviews/ratings: verified-buyer review (DELIVERED purchase) → PENDING → OPERATOR/ADMIN moderate approve/reject/hide/unhide; only PUBLISHED reviews public; Product.ratingAvg/reviewCount recomputed from PUBLISHED reviews; unique productId+userId), 22 (courier last-mile delivery of a dispatched replacement: OPERATOR/ADMIN assigns a DELIVERY partner via `replacement_assignments`; the partner drives it on `/delivery/replacement-tasks` and its `deliver` auto-completes the replacement DISPATCHED→COMPLETED — a non-money leg, audited by DELIVERY-actor ReturnEvents), 23 (catalog console UI: a connected React/Vite app `catalog-console/` giving SELLER product authoring + OPERATOR/ADMIN catalog-publishing review in one role-aware app, speaking through the Vite `/api` proxy), 24 (customer storefront UI: a connected React/Vite app `customer-storefront/` that browses the live public catalog, shows product detail with PUBLISHED reviews + rating aggregate, and offers CUSTOMER sign-in with a verified-buyer review-write surface and "my reviews" page), 25 (customer commerce UI: guest + signed-in cart with line-level update/remove, checkout preview, order placement (COD & PREPAID), COD OTP authorization, "my orders"/order detail, plus a dev-only sandbox capture harness that keeps the webhook secret server-side), 26 (staff review-moderation UI: an OPERATOR/ADMIN "Review Moderation" tab in the catalog console that lists PENDING/PUBLISHED/REJECTED/HIDDEN customer reviews and moderates them — approve/reject (pending), hide/unhide (published/hidden) — driving the live /product-reviews routes), 27 (SELLER sales & payouts dashboard: a SELLER "Sales & Payouts" tab in the catalog console over the live /seller/me, /seller/orders (+ accept/reject a PLACED slice), /seller/payables and /seller/settlements routes, with KPI cards + payables table), 28 (OPERATOR Operations console: an OPERATOR/ADMIN "Operations" tab in the catalog console with order-fulfilment (advance a live order) + returns (decide/pickup/inspection/refund/replacement) queues over new staff read endpoints /api/v1/ops/orders[/:id] and /api/v1/ops/returns[/:id], driving the existing fulfilment-advance and return-ops action routes; backend typecheck/build clean), 29 (DELIVERY courier task console: a DELIVERY-role "My Deliveries" tab in the catalog console running live courier tasks over /delivery/tasks (slice parcels) + /delivery/replacement-tasks (outbound replacements) with accept/reject(reason)/pickup/out-for-delivery/deliver/fail(reason) bound to the courier's own profile; the slice-task courier surface was enriched so it is not hollow — a new DELIVERY-only GET /delivery/tasks/:assignmentId returns orderNumber/paymentMethod/sellerName/sellerCode + items[] (productName/weight/sku/qty) + customer{name,phone,address} + events, and the partnerTasks list adds orderNumber/itemCount/customer; backend typecheck/build clean, console vite build clean), 30 (pluggable courier-provider integration: a Razorpay-style `courier` seam — `src/commerce/courier` `CourierProvider` (createShipment/track/confirmDelivery) with a default deterministic Sandbox provider and a real REST HTTP provider selected via `COURIER_PROVIDER` + `COURIER_BASE_URL`/`COURIER_API_KEY`; courier `pickup` books a provider waybill (tracking) and `deliver` captures a POD, persisted on delivery_assignments + replacement_assignments via migration `20260908190000_courier_tracking`; assignment serializers expose carrier/trackingNumber/trackingUrl/podRef/podSignedBy/podAt; verified live over sandbox AND real-HTTP against a local courier-protocol mock; non-money — per-slice payout deferred, seller earn stays order-level; 22 suites / 214 tests; typecheck/build clean), 31 (courier tracking-read surface: a read-only `GET /orders/:id/tracking` aggregates an order's slice-parcel + replacement courier legs (latest per slice/replacement) and calls the configured `CourierProvider.track` LIVE per waybill, merged with local leg state + POD; order owner CUSTOMER or OPERATOR/ADMIN may read (cross-owner 404, wrong role 403); connected customer-storefront OrderView "Track delivery" timeline; read-only NON-money; 23 suites / 222 tests; typecheck/build clean; 14/14 live HTTP-provider E2E), 32 (courier delivery-fee payout / money leg: CourierPayout EARNED→SETTLED ledger on actually-delivered courier legs, parcel ₹35 / replacement ₹40 via COURIER_FEE_* env, accrued atomically in the deliver tx + idempotent per assignment; DELIVERY self-service GET /delivery/payouts + OPERATOR/ADMIN list/summary/settle; roles enforced; DI fix for @Optional courierPayout injection; additive migration 20260908210000_courier_payout), 33 (infra packaging + baseline security hardening: CI/CD GitHub Actions + Docker/prod-compose containerisation landed just before and pushed `d38ea0c`; this session added always-on security response headers via a global securityHeadersMiddleware and an in-memory sliding-window rate limiter (@Throttle+ThrottleGuard, HTTP 429, keyed ip|method|route) on credential/account-creation/OTP endpoints — register/seller-register 10/hr, login 8/min, refresh 60/min, COD otp+verify 5/min — with baseline bcrypt/strong-password/whitelist-validation/generic-login-error/JWT-session revocability confirmed present; new throttle.guard.spec.ts + security-headers.middleware.spec.ts; 26 suites / 238 tests; typecheck/build clean), 34 (back-office Finance console: an OPERATOR/ADMIN catalog-console “Finance” tab unifying courier-payout settlement (/delivery/payouts summary/all/settle — Session 32) and the seller payables/settlements ledger (/finance payables/adjust/settlements/create/advance + reconciliation + report totals — Sessions 12/13): Overview & Reconciliation, Courier Payouts (per-partner Settle EARNED→SETTLED), Seller Payables (signed adjustment form), Seller Settlements (create grouped by seller + advance with only legal next states); frontend-only over existing OPERATOR/ADMIN routes; scripts/e2e-finance.mjs 32/32 live E2E with a throwaway money round-trip + full DB self-clean), 35 (DELIVERY-partner management console: an OPERATOR/ADMIN catalog-console “Delivery Partners” tab that registers a DELIVERY-role candidate as a delivery partner (partnerCode auto / vehicleType), toggles ACTIVE/SUSPENDED, filters a partner’s assignments with expandable detail+events, and reads the courier-payout summary/ledger; additive staff read GET /delivery/partner-candidates (DELIVERY users without a partner profile) + DeliveryService.listPartnerCandidates + 1 unit test; no schema change; scripts/e2e-deliveryops.mjs 22/22 live E2E with a throwaway onboard/status-toggling round-trip + full DB self-clean) || Completed sessions | 00–04, upgrade pass, 05 (buy-now + payments + COD), 06 (fulfilment/delivery), 07 (returns/refunds), 08 (item-level returns + pickup/inspection lifecycle), 09 (seller orgs + split-checkout + seller-ops core), 10 (per-seller fulfilment gating + delivery markers), 11 (partial fulfilment resolution of rejected seller slices), 12 (seller payables & settlements), 13 (finance reconciliation + reporting), 14 (seller onboarding/KYC + Organizations + REVIEWER), 15 (per-slice courier delivery + DELIVERY role), 16 (return replacement vs refund + evidence upload), 17 (outbound replacement dispatch), 18 (real Razorpay gateway + live refund execution E2E), 19 (Razorpay async refund reconciliation: `refund.processed`/`refund.failed` webhooks finalise in-flight PROCESSING refunds), 20 (product listing/publishing: seller-authored DRAFT → PENDING_REVIEW → OPERATOR/ADMIN approve/reject with surfaced reason; APPROVED+LIVE publishes into the existing public catalog; product_status_history audit), 21 (product reviews/ratings: verified-buyer review (DELIVERED purchase) → PENDING → OPERATOR/ADMIN moderate approve/reject/hide/unhide; only PUBLISHED reviews public; Product.ratingAvg/reviewCount recomputed from PUBLISHED reviews; unique productId+userId), 22 (courier last-mile delivery of a dispatched replacement: OPERATOR/ADMIN assigns a DELIVERY partner via `replacement_assignments`; the partner drives it on `/delivery/replacement-tasks` and its `deliver` auto-completes the replacement DISPATCHED→COMPLETED — a non-money leg, audited by DELIVERY-actor ReturnEvents), 23 (catalog console UI: a connected React/Vite app `catalog-console/` giving SELLER product authoring + OPERATOR/ADMIN catalog-publishing review in one role-aware app, speaking through the Vite `/api` proxy), 24 (customer storefront UI: a connected React/Vite app `customer-storefront/` that browses the live public catalog, shows product detail with PUBLISHED reviews + rating aggregate, and offers CUSTOMER sign-in with a verified-buyer review-write surface and "my reviews" page), 25 (customer commerce UI: guest + signed-in cart with line-level update/remove, checkout preview, order placement (COD & PREPAID), COD OTP authorization, "my orders"/order detail, plus a dev-only sandbox capture harness that keeps the webhook secret server-side), 26 (staff review-moderation UI: an OPERATOR/ADMIN "Review Moderation" tab in the catalog console that lists PENDING/PUBLISHED/REJECTED/HIDDEN customer reviews and moderates them — approve/reject (pending), hide/unhide (published/hidden) — driving the live /product-reviews routes), 27 (SELLER sales & payouts dashboard: a SELLER "Sales & Payouts" tab in the catalog console over the live /seller/me, /seller/orders (+ accept/reject a PLACED slice), /seller/payables and /seller/settlements routes, with KPI cards + payables table), 28 (OPERATOR Operations console: an OPERATOR/ADMIN "Operations" tab in the catalog console with order-fulfilment (advance a live order) + returns (decide/pickup/inspection/refund/replacement) queues over new staff read endpoints /api/v1/ops/orders[/:id] and /api/v1/ops/returns[/:id], driving the existing fulfilment-advance and return-ops action routes; backend typecheck/build clean), 29 (DELIVERY courier task console: a DELIVERY-role "My Deliveries" tab in the catalog console running live courier tasks over /delivery/tasks (slice parcels) + /delivery/replacement-tasks (outbound replacements) with accept/reject(reason)/pickup/out-for-delivery/deliver/fail(reason) bound to the courier's own profile; the slice-task courier surface was enriched so it is not hollow — a new DELIVERY-only GET /delivery/tasks/:assignmentId returns orderNumber/paymentMethod/sellerName/sellerCode + items[] (productName/weight/sku/qty) + customer{name,phone,address} + events, and the partnerTasks list adds orderNumber/itemCount/customer; backend typecheck/build clean, console vite build clean), 30 (pluggable courier-provider integration: a Razorpay-style `courier` seam — `src/commerce/courier` `CourierProvider` (createShipment/track/confirmDelivery) with a default deterministic Sandbox provider and a real REST HTTP provider selected via `COURIER_PROVIDER` + `COURIER_BASE_URL`/`COURIER_API_KEY`; courier `pickup` books a provider waybill (tracking) and `deliver` captures a POD, persisted on delivery_assignments + replacement_assignments via migration `20260908190000_courier_tracking`; assignment serializers expose carrier/trackingNumber/trackingUrl/podRef/podSignedBy/podAt; verified live over sandbox AND real-HTTP against a local courier-protocol mock; non-money — per-slice payout deferred, seller earn stays order-level; 22 suites / 214 tests; typecheck/build clean), 31 (courier tracking-read surface: a read-only `GET /orders/:id/tracking` aggregates an order's slice-parcel + replacement courier legs (latest per slice/replacement) and calls the configured `CourierProvider.track` LIVE per waybill, merged with local leg state + POD; order owner CUSTOMER or OPERATOR/ADMIN may read (cross-owner 404, wrong role 403); connected customer-storefront OrderView "Track delivery" timeline; read-only NON-money; 23 suites / 222 tests; typecheck/build clean; 14/14 live HTTP-provider E2E), 32 (courier delivery-fee payout / money leg: CourierPayout EARNED→SETTLED ledger on actually-delivered courier legs, parcel ₹35 / replacement ₹40 via COURIER_FEE_* env, accrued atomically in the deliver tx + idempotent per assignment; DELIVERY self-service GET /delivery/payouts + OPERATOR/ADMIN list/summary/settle; roles enforced; DI fix for @Optional courierPayout injection; additive migration 20260908210000_courier_payout), 33 (infra packaging + baseline security hardening: CI/CD GitHub Actions + Docker/prod-compose containerisation landed just before and pushed `d38ea0c`; this session added always-on security response headers via a global securityHeadersMiddleware and an in-memory sliding-window rate limiter (@Throttle+ThrottleGuard, HTTP 429, keyed ip|method|route) on credential/account-creation/OTP endpoints — register/seller-register 10/hr, login 8/min, refresh 60/min, COD otp+verify 5/min — with baseline bcrypt/strong-password/whitelist-validation/generic-login-error/JWT-session revocability confirmed present; new throttle.guard.spec.ts + security-headers.middleware.spec.ts; 26 suites / 238 tests; typecheck/build clean), 34 (back-office Finance console: an OPERATOR/ADMIN catalog-console “Finance” tab unifying courier-payout settlement (/delivery/payouts summary/all/settle — Session 32) and the seller payables/settlements ledger (/finance payables/adjust/settlements/create/advance + reconciliation + report totals — Sessions 12/13): Overview & Reconciliation, Courier Payouts (per-partner Settle EARNED→SETTLED), Seller Payables (signed adjustment form), Seller Settlements (create grouped by seller + advance with only legal next states); frontend-only over existing OPERATOR/ADMIN routes; scripts/e2e-finance.mjs 32/32 live E2E with a throwaway money round-trip + full DB self-clean) || Completed sessions | 00–04, upgrade pass, 05 (buy-now + payments + COD), 06 (fulfilment/delivery), 07 (returns/refunds), 08 (item-level returns + pickup/inspection lifecycle), 09 (seller orgs + split-checkout + seller-ops core), 10 (per-seller fulfilment gating + delivery markers), 11 (partial fulfilment resolution of rejected seller slices), 12 (seller payables & settlements), 13 (finance reconciliation + reporting), 14 (seller onboarding/KYC + Organizations + REVIEWER), 15 (per-slice courier delivery + DELIVERY role), 16 (return replacement vs refund + evidence upload), 17 (outbound replacement dispatch), 18 (real Razorpay gateway + live refund execution E2E), 19 (Razorpay async refund reconciliation: `refund.processed`/`refund.failed` webhooks finalise in-flight PROCESSING refunds), 20 (product listing/publishing: seller-authored DRAFT → PENDING_REVIEW → OPERATOR/ADMIN approve/reject with surfaced reason; APPROVED+LIVE publishes into the existing public catalog; product_status_history audit), 21 (product reviews/ratings: verified-buyer review (DELIVERED purchase) → PENDING → OPERATOR/ADMIN moderate approve/reject/hide/unhide; only PUBLISHED reviews public; Product.ratingAvg/reviewCount recomputed from PUBLISHED reviews; unique productId+userId), 22 (courier last-mile delivery of a dispatched replacement: OPERATOR/ADMIN assigns a DELIVERY partner via `replacement_assignments`; the partner drives it on `/delivery/replacement-tasks` and its `deliver` auto-completes the replacement DISPATCHED→COMPLETED — a non-money leg, audited by DELIVERY-actor ReturnEvents), 23 (catalog console UI: a connected React/Vite app `catalog-console/` giving SELLER product authoring + OPERATOR/ADMIN catalog-publishing review in one role-aware app, speaking through the Vite `/api` proxy), 24 (customer storefront UI: a connected React/Vite app `customer-storefront/` that browses the live public catalog, shows product detail with PUBLISHED reviews + rating aggregate, and offers CUSTOMER sign-in with a verified-buyer review-write surface and "my reviews" page), 25 (customer commerce UI: guest + signed-in cart with line-level update/remove, checkout preview, order placement (COD & PREPAID), COD OTP authorization, "my orders"/order detail, plus a dev-only sandbox capture harness that keeps the webhook secret server-side), 26 (staff review-moderation UI: an OPERATOR/ADMIN "Review Moderation" tab in the catalog console that lists PENDING/PUBLISHED/REJECTED/HIDDEN customer reviews and moderates them — approve/reject (pending), hide/unhide (published/hidden) — driving the live /product-reviews routes), 27 (SELLER sales & payouts dashboard: a SELLER "Sales & Payouts" tab in the catalog console over the live /seller/me, /seller/orders (+ accept/reject a PLACED slice), /seller/payables and /seller/settlements routes, with KPI cards + payables table), 28 (OPERATOR Operations console: an OPERATOR/ADMIN "Operations" tab in the catalog console with order-fulfilment (advance a live order) + returns (decide/pickup/inspection/refund/replacement) queues over new staff read endpoints /api/v1/ops/orders[/:id] and /api/v1/ops/returns[/:id], driving the existing fulfilment-advance and return-ops action routes; backend typecheck/build clean), 29 (DELIVERY courier task console: a DELIVERY-role "My Deliveries" tab in the catalog console running live courier tasks over /delivery/tasks (slice parcels) + /delivery/replacement-tasks (outbound replacements) with accept/reject(reason)/pickup/out-for-delivery/deliver/fail(reason) bound to the courier's own profile; the slice-task courier surface was enriched so it is not hollow — a new DELIVERY-only GET /delivery/tasks/:assignmentId returns orderNumber/paymentMethod/sellerName/sellerCode + items[] (productName/weight/sku/qty) + customer{name,phone,address} + events, and the partnerTasks list adds orderNumber/itemCount/customer; backend typecheck/build clean, console vite build clean), 30 (pluggable courier-provider integration: a Razorpay-style `courier` seam — `src/commerce/courier` `CourierProvider` (createShipment/track/confirmDelivery) with a default deterministic Sandbox provider and a real REST HTTP provider selected via `COURIER_PROVIDER` + `COURIER_BASE_URL`/`COURIER_API_KEY`; courier `pickup` books a provider waybill (tracking) and `deliver` captures a POD, persisted on delivery_assignments + replacement_assignments via migration `20260908190000_courier_tracking`; assignment serializers expose carrier/trackingNumber/trackingUrl/podRef/podSignedBy/podAt; verified live over sandbox AND real-HTTP against a local courier-protocol mock; non-money — per-slice payout deferred, seller earn stays order-level; 22 suites / 214 tests; typecheck/build clean), 31 (courier tracking-read surface: a read-only `GET /orders/:id/tracking` aggregates an order's slice-parcel + replacement courier legs (latest per slice/replacement) and calls the configured `CourierProvider.track` LIVE per waybill, merged with local leg state + POD; order owner CUSTOMER or OPERATOR/ADMIN may read (cross-owner 404, wrong role 403); connected customer-storefront OrderView "Track delivery" timeline; read-only NON-money; 23 suites / 222 tests; typecheck/build clean; 14/14 live HTTP-provider E2E), 32 (courier delivery-fee payout / money leg: CourierPayout EARNED→SETTLED ledger on actually-delivered courier legs, parcel ₹35 / replacement ₹40 via COURIER_FEE_* env, accrued atomically in the deliver tx + idempotent per assignment; DELIVERY self-service GET /delivery/payouts + OPERATOR/ADMIN list/summary/settle; roles enforced; DI fix for @Optional courierPayout injection; additive migration 20260908210000_courier_payout), 33 (infra packaging + baseline security hardening: CI/CD GitHub Actions + Docker/prod-compose containerisation landed just before and pushed `d38ea0c`; this session added always-on security response headers via a global securityHeadersMiddleware and an in-memory sliding-window rate limiter (@Throttle+ThrottleGuard, HTTP 429, keyed ip|method|route) on credential/account-creation/OTP endpoints — register/seller-register 10/hr, login 8/min, refresh 60/min, COD otp+verify 5/min — with baseline bcrypt/strong-password/whitelist-validation/generic-login-error/JWT-session revocability confirmed present; new throttle.guard.spec.ts + security-headers.middleware.spec.ts; 26 suites / 238 tests; typecheck/build clean), 34 (back-office Finance console: an OPERATOR/ADMIN catalog-console “Finance” tab unifying courier-payout settlement (/delivery/payouts summary/all/settle — Session 32) and the seller payables/settlements ledger (/finance payables/adjust/settlements/create/advance + reconciliation + report totals — Sessions 12/13): Overview & Reconciliation, Courier Payouts (per-partner Settle EARNED→SETTLED), Seller Payables (signed adjustment form), Seller Settlements (create grouped by seller + advance with only legal next states); frontend-only over existing OPERATOR/ADMIN routes; scripts/e2e-finance.mjs 32/32 live E2E with a throwaway money round-trip + full DB self-clean), 35 (DELIVERY-partner management console: an OPERATOR/ADMIN catalog-console “Delivery Partners” tab that registers a DELIVERY-role candidate as a delivery partner (partnerCode auto / vehicleType), toggles ACTIVE/SUSPENDED, filters a partner’s assignments with expandable detail+events, and reads the courier-payout summary/ledger; additive staff read GET /delivery/partner-candidates (DELIVERY users without a partner profile) + DeliveryService.listPartnerCandidates + 1 unit test; no schema change; scripts/e2e-deliveryops.mjs 22/22 live E2E with a throwaway onboard/status-toggling round-trip + full DB self-clean) , 36 (courier tracking in the STAFF consoles: surface live courier tracking in both back-office consoles over the Session 31 tracking data — the DELIVERY “My Deliveries” console gains a Live-tracking control on each expanded parcel/replacement task via new DELIVERY-only per-task reads GET /delivery/tasks/:assignmentId/tracking and GET /delivery/replacement-tasks/:assignmentId/tracking (CourierTrackingService.trackTaskForDelivery, bound to the courier's own partner: foreign/other-partner 404, no-partner 403); the OPERATOR Operations console gains a Track-delivery control per expanded order reusing GET /orders/:id/tracking; both render the shared TrackPanel (live provider events + local milestones + POD); additive backend, 4 new unit tests (26 suites/243 tests), scripts/e2e-staff-tracking.mjs 14/14 read-only live E2E + DB unchanged) |
| Next session | Session 38 (owner-chosen). Sessions 23–37 done & pushed (`origin/main`, see git log). Open candidates: the **auto-cycle payout schedule** option the owner did not pick in S37 (recorded as pending), courier/seller payout **notifications** (push/SMS/email), DELIVERY-partner payout **preferences/banking** (UPI/bank), tracking push/SMS/email notifications, or another owner-chosen priority. HTTPS/WAF/real-secrets/Redis-rate-limit/CSP remain ops/deploy-deferred (recorded in the Session 33 ledger entry)
| Active blockers | None local; Sessions 22, 23 (code) & 23/24 docs pushed to GitHub (`origin/main`, see git log) |
| Known technical debt | See `COMPLETION-MATRIX.md`; `src/generated/` gitignored; payments select the gateway via `PAYMENT_GATEWAY_PROVIDER` (default **sandbox**, unchanged) — **Session 18 adds a real Razorpay provider** (`RazorpayGateway`, real REST over Node fetch, Basic auth, amount in paise, `RAZORPAY_KEY_ID`/`KEY_SECRET`/`WEBHOOK_SECRET`, `RAZORPAY_BASE_URL` override) behind a `PaymentGateway` interface, plus **live refund execution** in `ReturnsService.completeRefund` that actually calls the gateway refund API and records the real `gatewayRef`/provider on the Refund + a `RefundTransaction` (replacing the hardcoded sandbox seq). E2E is verified against a **local Razorpay-protocol HTTP mock** (`scripts/razorpay-mock.mjs`) speaking the real wire protocol; flipping to real Razorpay only needs env base-URL/keys (no code change). Roles: OPERATOR/ADMIN run fulfilment/returns/finance; REVIEWER (onboarding/KYC only); DELIVERY (courier tasks only); SELLER accepts/rejects slices, reads own payables/settlements and runs own onboarding. Per-slice courier delivery (Session 15) is a real delivery_assignments model + DELIVERY role but **rides the order-level DELIVERED money trigger** (owner decision): a courier finalizing the last slice earns the accepted slices' payables exactly as before — per-slice earn is intentionally NOT used; real courier-provider integration/tracking/POD and per-slice payout are future work. Seller payables auto-earn at order DELIVERED (commission = per-seller `commissionRateBps`; Bilokat 0, partner 10%); payables/settlement DB-authoritative; completed refunds auto-debit returned goods' net off the EARNED payable (net-zero rule) as SYSTEM `seller_payable_adjustment` (in/after-settlement payables untouched, surfaced by reconciliation). Return replacement/exchange (Session 16) is offered as a terminal `REPLACEMENT_ISSUED` resolution on the item-level returns flow (no Refund, no seller payable auto-debit — the refund/auto-debit money logic is kept intact for REFUND-resolution requests); evidence upload (customer + operator) stores an object reference only. **Outbound replacement dispatch (Session 17)** drives the `ReplacementStatus` lifecycle `PENDING_DISPATCH → DISPATCHED → COMPLETED | CANCELLED` via OPERATOR/ADMIN endpoints, each step audited via a per-request `ReturnEvent` (REPLACEMENT_DISPATCHED/COMPLETED/CANCELLED) with dispatch reference/note/cancel reason persisted; it is a **non-money** leg (no ledger, no seller debit). **Courier last-mile delivery of the dispatched replacement (Session 22)** is now real: OPERATOR/ADMIN assign a DELIVERY partner to a DISPATCHED replacement (`replacement_assignments`, one active at a time) and the DELIVERY partner drives it on `/delivery/replacement-tasks` (accept/reject/pickup/out-for-delivery/fail); the courier's **`deliver` auto-completes the replacement DISPATCHED→COMPLETED** in one tx (non-money, audited as DELIVERY-actor `ReturnEvent`s). The Session 17 OPERATOR manual `/complete` now returns 409 while a courier is assigned. Product listing/publishing (Session 20) is done. **Product reviews/ratings (Session 21) now drive `Product.ratingAvg`/`reviewCount` from PUBLISHED (approved) reviews**: a product with at least one approved review shows its true aggregate; a product that has never had an approved review keeps its seeded marketing summary until the first review is approved (approve/unhide recomputes; rejecting a never-published review does NOT touch the summary; hiding removes it). No real payout execution. Reconciliation read-only. |
| Last verification | typecheck/build OK; `npm test` **205 passing (21 suites)** (Session 22 added `src/commerce/replacement-courier.service.spec.ts`); **24 migrations applied** (`migrate status` up to date). **Session 22 E2E** (`scripts/e2e-replacement-courier.mjs`, sandbox API `:4500`, **31/31 ok**): fresh PREPAID order → sandbox capture → delivered; customer REPLACEMENT return → inspection PASS → `REPLACEMENT_ISSUED` + replacement PENDING_DISPATCH → operator dispatch → **assign courier** (DELIVERY `delivery15`, `replacement_assignments` ASSIGNED); negatives (duplicate assign 409, OPERATOR manual `/complete` 409, CUSTOMER/courier/OPERATOR RBAC 403s); courier accept→pickup→out-for-delivery→**deliver** auto-completes the replacement (DISPATCHED→COMPLETED) + assignment DELIVERED; return read shows COMPLETED + DELIVERED assignment + `refund:null`; original order unchanged (DELIVERED/PAID). E2E rows cleaned up. Live Session 20 E2E (`scripts/e2e-publishing.mjs`, 13/13) and **Session 21 E2E** (`scripts/e2e-reviews.mjs` on a fresh `:4400` API, **17/17 ok**). **Session 23 & 24 frontends verified live** against the running API through each Vite `/api` proxy (`npm run build` clean in both `catalog-console/` and `customer-storefront/`): Session 23 drove a real seller→submit→staff-approve round-trip and staff product detail (history + timestamps), then test rows were cleaned up; Session 24 read live public catalog/detail/reviews and drove a CUSTOMER login (`s12@example.com`) + review submit → PENDING (not public), then deleted the review to keep data clean. **Session 25 commerce E2E passed live** (`/tmp/e2e-commerce.mjs`, **21/21 ok**) on a throwaway CUSTOMER: guest cart (no auth), signed-in cart with line `id`s, server-authoritative checkout preview, place COD order (COD_PENDING) → cart empties → COD OTP via sandbox `devOtp` → verified (CONFIRMED); re-add → place PREPAID order (PENDING) → payment intent (reference/amount) → **dev sandbox capture harness** (`POST /dev/orders/:id/sandbox-capture`, guarded to non-production, secret kept server-side) → payment CONFIRMED + order PAID; re-capture → 400. Both orders cancelled (stock restored) and all `c25@` throwaway buyers/orders cleaned from the DB: buyer `s12@example.com` (CUSTOMER with a DELIVERED order containing `ratlami-sev`, APPROVED+LIVE) `POST /reviews` → PENDING + verifiedBuyer → shows in `/reviews/me`, NOT public (0 published); duplicate → 409; a freshly-registered CUSTOMER (no delivery) → 403 and SELLER (non-CUSTOMER role) → 403; OPERATOR `pfop@example.com` sees it in `/product-reviews?status=PENDING` (CUSTOMER → 403) → **approve** → PUBLISHED + moderationNote, now public and `Product.ratingAvg`/`reviewCount` recomputed to 5/1; **hide** → not public + aggregate 0/0; **unhide** → PUBLISHED restored; a second review on `shahi-kaju-mixture` **reject** → REJECTED with reason (its product summary untouched, stays 4.9/2450); **editing** the REJECTED review reopens it to PENDING. E2E review rows cleaned up after the run and product summaries reset to seeded values (ratlami-sev 4.9/3820). **Session 26 review-moderation E2E passed live** (through the catalog-console proxy; `s12@example.com` buyer + `pfop@example.com` OPERATOR): submit 2 reviews → PENDING → `/product-reviews` PENDING list shows both, detail OK → reject one (REJECTED with reason, product aggregate untouched) → approve the other (PUBLISHED) → hide (HIDDEN) → unhide (PUBLISHED); a CUSTOMER hitting `/product-reviews` → 403. Test reviews deleted and the two products' aggregates reset to seed (ratlami-sev 4.9/3820, shahi-kaju-mixture 4.9/2450). **Session 27 SELLER dashboard verified live** through the console proxy (`seller1@example.com` / `SELL-BILOKAT`): `/seller/me`, `/seller/orders` (8 slices, 1 PLACED + 7 ACCEPTED), `/seller/payables` (6 EARNED), `/seller/settlements` (0) all return the shapes the dashboard renders; an OPERATOR on `/seller/orders` → 403. seller1's single real PLACED slice was deliberately NOT accept/rejected (would irreversibly change live data); the accept/reject mutation paths are covered by backend unit tests (`seller-ops.service.spec.ts` + `settlement.service.spec.ts`, 32/32). **Session 28 OPERATOR ops queue E2E passed live** (`/tmp/e2e-ops.mjs`, **18/18 ok**, OPERATOR `pfop@example.com`): `/ops/orders` (total 8, row has number/status/payment/price/items/sellerOrders), status filter `?status=PACKED` (total 1), `/ops/orders/:id` detail; `/ops/returns` (total 8, full serializer: status/resolution/reasonCode/items/evidence/refund/replacement+assignments; both REFUND & REPLACEMENT resolutions present), status filter COMPLETED (6), `/ops/returns/:id` detail; a CUSTOMER (`s12@example.com`) gets **403 on every /ops queue + detail** (OPERATOR/ADMIN-only RBAC). Console action-wiring confirmed (`/tmp/e2e-ops-wire.mjs`, **4/4 ok**): firing the console's own action calls at terminal rows returns clean 409s — POST /orders/:id/fulfilment/advance on a REFUNDED order → "Cannot transition from REFUNDED to SHIPPED", and /return-requests/:id/decision + /pickup on a COMPLETED return → state-guard 409s — proving the UI buttons hit the real per-id routes. The one real PACKED order was left untouched (no advance on live data). Full delivery/return/refund mutation scenarios are not run on live rows (would create seller payables / ledger), so the catalogue-console "Operations" tab was verified as read-queues + correct route wiring, with the mutation paths covered by backend tests. Vite build clean; module hot-reloaded (200). **Session 29 DELIVERY courier console verified live** (API `:4600`, console proxy `:5173`; `delivery15@example.com`/DELIVERY): RBAC read probes (DELIVERY reads its own `/delivery/tasks` + `/delivery/replacement-tasks`; bogus detail 404; DELIVERY on OPERATOR `/delivery/*` admin lists 403; OPERATOR reads admin lists but hitting the courier task surface 403; cross-role slice-task detail 403; `fail` w/o reason 400). Enriched slice-task detail returned orderNumber/paymentMethod/sellerName/sellerCode/items[]/customer{name,phone,address}+events (non-hollow); replacement detail returned replacementReference/quantityTotal/customer. Demo rows seeded via `scripts/seed-courier-demo.mjs` and left actionable (courier-accepted, not delivered): slice parcel `DLVA-08BA1C54` ACCEPTED (BK-MTS7EXB5) + replacement `RDLA-996C06BF` ACCEPTED (RPL-MTS7EXQS-WGP3), both shown in the console through the proxy. Backend typecheck/build clean; console `npm run build` clean.  Prior Session 14–27 E2E remain valid (2026-09-08). **Session 30 verified live (tracking + POD):** `prisma migrate deploy` → 25 migrations; `tsc` + `nest build` clean; `npx jest` 22 suites / 214 tests. Sandbox default: picking up the demo slice parcel + replacement booked `SWB-…` (`Sandbox Courier`) with a `SHIPMENT_BOOKED` DeliveryEvent. Real-HTTP provider (`/tmp/s30_http_e2e.mjs`, **8/8 ok**, API `:4700` `COURIER_PROVIDER=http COURIER_BASE_URL=http://localhost:9600` against `scripts/courier-mock.mjs`): slice + replacement pickup booked `MCK-…` waybills over real HTTP; replacement out-for-delivery → deliver captured `POD-MCK-84F6EF3E` and auto-completed (DELIVERED+POD+COMPLETED persisted); mock `/_state` + `/track` DELIVERED. All courier steps non-money (no seller payable / Refund); slice parcel left PICKED_UP. **Session 32 verified live (money leg)** (`npx jest` 24 suites / 233 tests; `/tmp/s32_payout_e2e.mjs` **18/18** on sandbox API `:4600`): fresh PREPAID order shipped; slice courier assigned+accepted; pickup/out-for-delivery/**deliver** accrued a **EARNED ₹35 parcel** courier payout (idempotent per assignment); DELIVERY `GET /delivery/payouts` + summary showed it; OPERATOR `GET /delivery/payouts/all?status=EARNED` + `/summary` showed it; `POST /delivery/payouts/settle {deliveryPartnerId}` → `{settled:1,totalAmount:35}` and the row moved to **SETTLED** (settledAt) with summary paid ≥35; OPERATOR→DELIVERY route 403 and DELIVERY→OPERATOR route 403. Money cleaned (courier_payouts=0; S32 orders + their seller payables deleted; S29/S30 demo rows untouched). A DI bug surfaced live (union `| null` erased CourierPayoutService from `design:paramtypes`, so `@Optional` delivered `undefined`) was fixed with an explicit `@Inject`. |
| Repository health | pushed to GitHub (Sessions 28–31; see git log, `origin/main`); no committed secrets |

> ## IMPORTANT PRODUCT DECISION (owner)
> **`landing-page/` is a PROTOTYPE / UX reference only — NOT an exact pixel spec.**
> It shows the *feel* of user interactions and checkout the owner wants. It is
> NOT a strict "make it exactly this." Future customer experience should be
> enriched/built properly per the PROJECT-DOCS specs and later requirements, not
> slavishly copied from the prototype. The backend/DB remains the source of truth.

---

## What exists vs. what the specs require

The repository today is a **documentation + prototype container**, not the final
multi-app platform.

### Present
- `PROJECT-DOCS/` — all 13 specification documents + AI-PROGRESS control files.
- `landing-page/` — a **static single-page React marketing prototype** named
  "Bilokat" (namkeen/snacks D2C). All data is hardcoded. No API / backend calls.
- Backend (`src/`, `prisma/`) — NestJS + Prisma:
  - Session 01: config, health, envelope, request-id, Identity + Catalog data
    model + 3 migrations.
  - Session 02: **Auth + RBAC foundation** — register/login/logout/me,
    refresh-token rotation + reuse detection, `user_sessions` registry,
    JwtAuthGuard + RolesGuard, `@Roles`/`@CurrentUserId` decorators.
    Auth API live under `/api/v1/auth`.
  - Session 03: **Catalog API** (public) under `/api/v1/catalog` — categories,
    product list (filters: category/bestseller/new/search/price, sort, pagination),
    product detail by id/slug. Seed populates 5 categories + 8 products mirroring
    the landing-page catalog. Product schema gained display fields
    (originalPrice, weightLabel, ratingAvg, reviewCount, pairingSuggestion,
    customerFavTag). Migration `20260907120621_product_catalog_display_fields`.
  - Session 04: **Commerce — cart, checkout, orders** (server-authoritative):
    guest + authenticated carts with merge-on-login (`OptionalJwtAuthGuard` +
    `x-guest-session-id`), cart add/update/remove/clear under `/api/v1/cart`;
    checkout quote/preview that always re-derives price from DB product
    `basePrice` + asserts stock; transactional order placement (stock reserve via
    atomic decrement, coupon validate+usage++, address snapshot, order items,
    status history, cart → CONVERTED); list/get/cancel orders (cancel restores
    stock). Dev coupons seeded (`BILOKAT20`, `FLAT50`, `SAVE10`). Commerce tables
    via 2 migrations (`commerce_models`, `cart_status_merged`).
  - Session 05: **Buy-now + payments (COD & online intent/capture)**:
    `POST /buy-now` (direct no-cart order, same server-authoritative pricing);
    provider-agnostic `Payment` intent (`payments`/`payment_transactions`/
    `payment_webhooks`) created for PREPAID orders; signed **sandbox gateway**
    webhook `POST /payments/webhook/sandbox` (HMAC signature, idempotent via
    UNIQUE(provider, provider_event_id), amount match, capture → order `PAID`,
    transaction ledger, audit webhook log); COD secondary-mobile **OTP** flow
    (`/orders/:id/cod` otp/verify — only the OTP hash is stored, expires, attempt
    cap → REJECTED). Migration `payment_cod_models`.
  - Session 06: **Fulfilment/delivery state machine**: operator-only
    `POST /orders/:id/fulfilment/advance` (`@Roles(OPERATOR, ADMIN)` — customers
    cannot self-set delivery). Advance transitions PLACED→CONFIRMED→PACKED→SHIPPED→
    OUT_FOR_DELIVERY→DELIVERED with a confirmation gate (PREPAID must be PAID; COD
    must have passed OTP verification), legal-transition map + concurrency-safe
    guarded `updateMany(where status)`, an audited `order_status_history` row per
    transition (actor CONTROL + operator id + reason), and on DELIVERED sets
    `deliveredAt` + flips COD `COD_PENDING`→`COD_PAID` (cash collected at door).
    Customer timeline `GET /orders/:id/history`.
  - Sessions 07→08: **Returns & refunds** (DB-Design §§85-92). Session 07 built a
    whole-order flow (order → RETURN_REQUESTED/RETURNED/REFUND_PENDING/REFUNDED,
    `payment_transactions` REFUND); Session 08 **reworked it to item-level + full
    lifecycle** (the whole-order flow was superseded, not kept in parallel):
    customer `POST|GET /orders/:id/returns` now selects specific order items +
    quantities (customer order items expose `orderItemId`); proportional refund
    allocation by line value (full return of every line sums exactly to grand
    total). Lifecycle: request → operator decision (approve/reject, rejection
    reason mandatory) → schedulePickup → pickedUp → per-line inspection
    (PASS / PARTIAL_PASS 50% / FAIL 0, `return_inspections`) → auto
    `APPROVED_FOR_REFUND` (server-amounted per-item refund) → initiate refund
    (`GATEWAY` for PREPAID / `COD` for cash; never exceeds grand across partials)
    → sandbox complete → ReturnRequest `COMPLETED` + `refund_transactions` ledger
    + order only goes `REFUNDED`/`paymentStatus REFUNDED` once the aggregate of
    refunds covers the whole grand total (partial returns keep order `DELIVERED`).
    `return_events` record every step (customer + operator actors). Requests/
    decisions are operator-gated (`@Roles(OPERATOR,ADMIN)`).
  - Session 09: **Seller orgs + multi-seller catalog + split-checkout core**
    (scope "B", owner-selected). Schema/migration
    `20260907142140_seller_split_checkout`: `Seller` (sellers) + `SellerStatus`,
    `Product.sellerId` (required; seeded 2 sellers — Bilokat legacy + Rajrani
    partner; migration backfills existing products/orders under the legacy seller),
    `SellerOrder` (seller_orders) + `SellerOrderStatus`, `OrderItem.sellerOrderId`
    + `sellerNameSnapshot`, `User.sellerId` (SELLER-role operator binding). Order
    capture (`checkout` + `buy-now`) now splits each order into one `SellerOrder`
    per seller: the customer `Order` stays the single authoritative money/payment/
    refund entity, and each seller_order carries that seller's real subtotal plus a
    proportional share of order tax/discount/delivery/grand allocated by line-value,
    with the last seller absorbing rounding so **Σ seller_order.grandTotal ===
    order.grandTotal exactly**. Every order item now belongs to a seller_order and
    carries a seller-name snapshot. `Seller-ops` surface (`@Roles(SELLER)`,
    scoped to the caller's seller via `User.sellerId`): `/seller/me`,
    `/seller/products` (own catalog), `/seller/orders` (+ `/…/:id`) list the
    seller's slices with order context, and `accept`/`reject` (reason mandatory)
    drive the seller_order lifecycle PLACED→ACCEPTED/REJECTED. Order cancellation
    cascades open seller_orders → CANCELLED. Returns/refunds/fulfilment keep
    working on the order/orderItem level unchanged.
  - Session 10: **Per-seller fulfilment gating + delivery markers**. Migration
    `20260907143713_seller_fulfilment_markers` adds `shippedAt`/`deliveredAt` to
    seller_orders. The operator `fulfilment/advance` now enforces a per-seller
    **acceptance gate**: before an order may reach SHIPPED / OUT_FOR_DELIVERY /
    DELIVERED, every non-cancelled seller slice must be `ACCEPTED` — a still-`PLACED`
    or `REJECTED` slice blocks shipping and the error names the blockers. When the
    order is SHIPPED / DELIVERED the service stamps `shippedAt`/`deliveredAt` on the
    accepted slices (order-level delivery covers accepted slices together). New
    fields exposed on the order view and the seller-ops order projections.
    Returns/refunds/order-level money unchanged.
  - Session 11: **Partial fulfilment resolution of rejected seller slices**
    (owner-selected rule for a seller slice rejected before shipping: cancel the
    slice, let the remaining accepted sellers ship together as one order-level
    delivery, and for PREPAID refund the buyer that slice's share). Migration
    `20260907232834_partial_fulfilment_resolution` adds nullable
    `refunds.sellerOrderId` (FK → seller_orders) + index, makes
    `refunds.returnRequestId` nullable, and adds `seller_orders.cancelledAt` +
    `cancellationReason`. New OPERATOR/ADMIN endpoint
    `POST /orders/:id/slices/:sellerOrderId/resolve-reject` → `OrderService.
    resolveRejectedSlice`: in one transaction it requires the order status
    PLACED/CONFIRMED/PACKED (pre-shipment) and the slice `REJECTED`; for PREPAID it
    requires `paymentStatus=PAID`; then marks the slice `CANCELLED` (with
    cancelledAt + reason), releases that slice's items' stock only, and for PREPAID
    records one **COMPLETED** `Refund` (method GATEWAY, sandbox gatewayRef + SUCCESS
    `RefundTransaction`) for the slice's `grandTotal`, guarding cumulative refunds
    against the order grand total; an `order_status_history` CONTROL row records the
    resolution. `cancelOrder` is hardened so it never double-releases stock for an
    already-CANCELLED slice. The remaining ACCEPTED slices then pass the Session 10
    gate and ship/deliver together; a REJECTED/unresolved slice still blocks
    shipping. New projection fields expose `cancelledAt`/`cancellationReason` per
    slice.
  - Session 12: **Seller payables & settlements** (finance/settlement loop; owner
    chose rule "seller earns goods value minus commission", per-seller
    `commissionRateBps`, auto-earn at delivery). Migration
    `20260907234906_seller_payables_settlements` adds `Seller.commissionRateBps`
    (Bilokat 0, partner Rajrani 1000 bps = 10%) plus `seller_payables`,
    `seller_payable_adjustments`, `settlements`, `settlement_items`,
    `settlement_events` (14 migrations total). When an order reaches DELIVERED the
    fulfilment service atomically earns one **SellerPayable** per ACCEPTED slice
    (a Session-11 CANCELLED/REJECTED slice earns nothing): seller pays =
    `goodsValue(=subtotal−discount)` − commission; Bilokat retains tax + delivery,
    so net + commission + tax + delivery == slice grandTotal exactly. Money
    rounding is paise-safe. Operator/ADMIN finance surface `/api/v1/finance`
    (payables list/detail, add a signed audited **adjustment** to an EARNED
    payable, settlements list/detail/create, settlement `advance` through
    PENDING→APPROVED→PROCESSING→PAID→RECONCILED with PROCESSING→FAILED→retry);
    each payable can sit in only one settlement (unique `sellerPayableId`), and
    PAID marks its payables SETTLED. Seller-facing reads expose their own
    payables/settlements under `/seller`. Every step is audited via
    `settlement_events`/`seller_payable_adjustments`; no financial value is
    rewritten.
  - Session 14: **Seller onboarding/KYC + Organizations + REVIEWER role**
    (owner decision: full additive org model; application origin = BOTH self-service
    and operator-managed; onboarding approval state machine on Seller). Schema/migration
    `20260908100000_orgs_onboarding_kyc`: adds `Organization`/`OrganizationMember`
    (a SELLER-type org per seller; seller operator bound as an OWNER member), expands
    `SellerStatus` to `REGISTERED/PENDING/UNDER_REVIEW/APPROVED/REJECTED` (retaining
    `ACTIVE/SUSPENDED/DEACTIVATED`), adds `Seller.organizationId` + `activatedAt`, and
    tables `seller_applications` / `seller_documents` / `seller_reviews` /
    `seller_status_history`. `Seller.operators` (via `users.sellerId`) is retained as a
    denormalized convenience so all prior commerce code is untouched. New `REVIEWER`
    role (onboarding-only). Public `POST /auth/seller-register` (self-service): creates
    the SELLER org + a PENDING seller + a DRAFT application + an OWNER member, returns
    tokens. New `src/seller/` module + controllers: owner surface
    `/seller/onboarding/{me,profile,documents,submit}` (SELLER, bound to own seller;
    document uploads are **storage-intent only**, no object store), staff
    `/seller-onboarding/*` — `sellers` POST (OPERATOR/ADMIN create ACTIVE seller),
    `applications` list/detail/review (REVIEWER/ADMIN), `documents/:id/verify`
    (REVIEWER/ADMIN), `sellers/:id/activate` (REVIEWER/ADMIN/OPERATOR, APPROVED→ACTIVE),
    `sellers/:id/status` (OPERATOR/ADMIN operational). REVIEWER is denied
    finance/fulfilment/returns/seller-order/op-create routes; owner editing is blocked
    once ACTIVE; every status transition is audited in `seller_status_history`. Seed
    backfills sellers→orgs + OWNER membership.
  - Session 20: **Product listing/publishing** (owner-chosen next). A **SELLER**
    bound to an ACTIVE seller org (strictly `Product.sellerId`-scoped via a
    `requireSeller` that enforces SELLER role + `User.sellerId` + ACTIVE seller)
    authors a product **DRAFT** (`POST /seller/catalog`, slug auto-deduped) with
    catalog fields (name/slug/tagline/description/category[ACTIVE]/brand/
    `regionOrigin`/basePrice/originalPrice(MRP)/weightLabel/spiceLevel/ingredients/
    nutritional/pairingSuggestion/isBestseller/isNew/stockOnHand/stockStatus + media)
    in an active Category; edits a DRAFT/REJECTED (`PATCH`), submits it to
    **PENDING_REVIEW** (`POST …/submit`), lists/reads only its own products
    (`GET /seller/catalog` + `/:id`), and archives non-published ones. Nothing goes
    live without **OPERATOR/ADMIN** staff approval on a distinct review surface
    (`/catalog-publishing/products`, `@Roles(OPERATOR, ADMIN)`): approve →
    `APPROVED` + `visibility LIVE` + `publishedAt` → visible on the existing **public
    catalog** (whose filter stays `APPROVED`+`LIVE`+not-deleted — Session 20 only
    feeds it); reject → `REJECTED` + `HIDDEN` + mandatory reason surfaced to the
    seller (`reviewNote`); a REJECTED product may be edited + resubmitted → PENDING_REVIEW
    again. Every transition is written to an immutable **`product_status_history`**
    audit (actor role, from→to status, reason, time). Migration
    `20260908175000_product_publishing` (new `reviewNote` column on products +
    `product_status_history` table, productStatusHistory relation). Distinct routes
    mapped in `RouterExplorer` for `seller/catalog` (SELLER) and
    `catalog-publishing/products` (OPERATOR/ADMIN).
  - Session 21: **Product reviews & ratings (backend)** (owner-chosen next). A
    **verified buyer** — any authenticated CUSTOMER with a DELIVERED order item for
    the (APPROVED+LIVE) product — may post one review per product per user
    (`POST /reviews`, `@Roles(CUSTOMER)`, rating 1–5 + optional title/comment,
    `verifiedBuyer=true`, starts **PENDING**); duplicate → 409, non-buyer → 403,
    SELLER/non-CUSTOMER role → 403. Customer `GET /reviews/me`, `GET /reviews/:id`,
    `PATCH /reviews/:id` (edit own PENDING, or a REJECTED/HIDDEN one → reopens to
    PENDING for re-moderation; PUBLISHED is not customer-editable/removable), and
    `DELETE /reviews/:id` (own, non-PUBLISHED only). Staff moderation
    `ReviewModerationController @Controller('product-reviews') @Roles(OPERATOR, ADMIN)`
    (REVIEWER stays onboarding/KYC-only): list/detail + `approve` (PENDING →
    **PUBLISHED**, public + aggregate), `reject` (reason), `hide` (PUBLISHED → HIDDEN,
    drops from aggregate), `unhide` (restore). Each action stores `moderatorId`/
    `moderatedAt`/`moderationNote`. Only **PUBLISHED** reviews are exposed publicly via
    `PublicReviewsController GET /catalog/products/:identifier/reviews` (no auth,
    paginated, author name + `verifiedBuyer`); PENDING/REJECTED/HIDDEN never surface.
    `Product.ratingAvg`/`reviewCount` are recomputed in-transaction from PUBLISHED
    reviews on approve/hide/unhide (reject of a never-published review leaves the
    summary untouched). Migration `20260908200000_product_reviews` (+ moderation cols +
    `@@unique([productId,userId])` + `(userId,status)` index). New `src/reviews/` module.
  - Session 22: **Courier last-mile delivery of a dispatched replacement** (owner-chosen
    next). Once a REPLACEMENT resolution is terminal and OPERATOR **dispatches** the
    replacement (Session 17, `DISPATCHED`), OPERATOR/ADMIN now **assign a DELIVERY partner**
    (`POST /return-requests/:id/replacement/assign-courier`; migration
    `20260908203000_replacement_courier` adds the `replacement_assignments` table + a
    `(userId,status)`-style courier audit trail via new `ReturnEventType` courier events and
    `ReturnActorType.DELIVERY`; one active assignment per replacement). The DELIVERY partner
    drives it on `/delivery/replacement-tasks` (accept/reject/pickup/out-for-delivery/fail,
    own-profile only), and the courier's **`deliver`** marks the assignment DELIVERED and
    auto-completes the replacement `DISPATCHED → COMPLETED` in one transaction — a **non-money**
    leg (no Refund, no payable, original order untouched), audited by DELIVERY-actor
    `ReturnEvent`s on the return. The Session 17 OPERATOR manual `/complete` now returns 409
    while a courier is assigned. New `src/commerce/replacement-courier.*` (+ 12 unit tests).

### Absent (per spec) / deferred
- Seller onboarding/KYC + orgs (applications/documents/reviews/status-history +
  Organization/OrganizationMember + REVIEWER role) now **implemented** (Session 14).
  Still absent: per-slice shipment/delivery model or courier handoff, auto-cancel-all
  or reassign/retry, review/rating storefront UI, real delivery-courier
  integration, and no **real payout execution** (a settlement reaching PAID is a
  sandbox-marked ledger state; the external transfer is out of scope). Seller
  payables/settlements now exist as a DB-authoritative ledger (Session 12), and a
  completed customer refund auto-debits the returned goods' net earnings off the
  delivered slice's **EARNED** payable (Session 13, net-zero rule) as a SYSTEM
  `seller_payable_adjustment`; a payable already in/after a settlement is never
  auto-debited (surfaced by reconciliation instead), and `seller_amount`
  on seller_order remains a legacy gross-basis column (seller_payables is the
  authoritative settlement basis). Pre-shipment rejected-slice resolution path
  (Session 11 OPERATOR resolve-reject → CANCELLED + partial refund) still exists.
- Split-checkout core (multi-seller seller_orders + seller accept/reject + money
  reconciliation) is done (Session 09); seller_orders gate the order as follows
  (Sessions 10–12): operator shipping requires every non-cancelled seller slice to
  be ACCEPTED (blocked by PLACED/REJECTED), per-seller shippedAt/deliveredAt markers
  are stamped at order SHIPPED/DELIVERED, an OPERATOR can resolve a REJECTED slice
  to CANCELLED (partial-fulfilment cancel + PREPAID refund) so the remaining ACCEPTED
  slices ship together, and at DELIVERED each accepted slice auto-earns its seller
  payable (cancelled slices earn nothing) which OPERATOR rolls into settlements.
- Fulfilment/delivery/returns/refunds/payables/settlements exist as internal
  operator flows (`OPERATOR`/`ADMIN`); a SELLER role can accept/reject its slices,
  read its own payables/settlements, and complete its own onboarding/KYC (Session 14);
  a separate `REVIEWER` handles KYC. No dedicated finance/courier role or real courier
  handoff yet.
- Returns now support both a **refund** resolution and a **replacement (exchange)**
  resolution with customer photo evidence upload (Session 16): a customer picks
  `REFUND` (default) or `REPLACEMENT`; a REPLACEMENT request is `evidenceRequired`,
  and when inspection completes it goes terminal `REPLACEMENT_ISSUED` with a
  `replacement` (PENDING_DISPATCH) row and **no Refund / no seller-payable auto-debit**
  (money logic kept intact for REFUND requests). Session 17 finishes the outbound leg:
  OPERATOR drives `replacement` `PENDING_DISPATCH → DISPATCHED → COMPLETED | CANCELLED`
  (dispatch ref/note, cancel reason, all audited via `ReturnEvent`). Item-level partial
  returns, pickup scheduling, per-line inspection, and the `return_evidence`/
  `return_items`/`return_events`/`return_inspections`/`replacements`/`refund_transactions`
  records are modelled and working (Sessions 08 + 16 + 17).
- No ABAC/organization/tenant isolation, step-up auth (deferred to later auth work).
- No `customer-web` full application (multi-page with account, orders, etc.).
- No `seller-web`, `catalog-publishing-web`, `support-web`, `delivery-web`,
  `finance-web`, `control-web`, `analytics-web` applications.
- No Redis-backed caching/jobs, no event bus / outbox yet.
- Session 18 adds a real **Razorpay** payment-gateway provider (real REST over the
  Razorpay wire protocol via Node fetch, selectable with `PAYMENT_GATEWAY_PROVIDER`,
  default stays sandbox) with **live refund execution**, E2E-verified against a local
  Razorpay-protocol mock; live external Razorpay is a config flip (env base-URL/keys)
  only. No COD cash-collection at delivery, no settlement payout execution, no AI,
  no analytics, no courier-provider integration beyond the in-app DELIVERY role.
- No CI/CD, no Docker runtime available in sandbox (docker-compose file is
  written; Postgres 17 + Redis used locally). Only backend unit tests exist in-repo
  (Jest); live end-to-end checks are one-off Node drivers under `scripts/`
  (`e2e-*.mjs`), not part of `npm test`.

---

## Document priority (when specs conflict)
Per `00-MASTER-SPEC.md` §Document priority:
1. Explicit latest project decision
2. `00-MASTER-SPEC.md`
3. `01-ARCHITECTURE.md`
4. `02-BUSINESS-WORKFLOWS.md`
5. Database / API / Security specs
6. Feature-specific specs
7. Existing implementation

Existing code is **not** automatically correct.
